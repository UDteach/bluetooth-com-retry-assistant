[project]
name = "bluetooth-assistant"
version = "0.1.0"
description = "Windows Bluetooth pairing retry assistant for SPP COM port discovery."
requires-python = ">=3.10"
dependencies = ["pyserial>=3.5"]

[tool.pytest.ini_options]
testpaths = ["tests"]
